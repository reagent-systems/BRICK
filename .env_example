# X/Twitter OAuth 2.0 Credentials
# IMPORTANT: You need OAuth 2.0 credentials, NOT API Key/Secret
# 
# To get OAuth 2.0 credentials:
# 1. Go to https://developer.twitter.com/en/portal/dashboard
# 2. Open your app → "User authentication settings"
# 3. Enable "OAuth 2.0"
# 4. Set App permissions: Read and write
# 5. Set Type of App: Web App, Automated App or Bot
# 6. Add Callback URI: http://localhost:5173/auth/twitter/callback
# 7. Save → You'll see "OAuth 2.0 Client ID" and "OAuth 2.0 Client Secret"
#
# NOTE: API Key/Secret and Bearer Token are for OAuth 1.0a (not what we need)
VITE_TWITTER_CLIENT_ID=your_oauth2_client_id_here
VITE_TWITTER_CLIENT_SECRET=your_oauth2_client_secret_here

# OAuth Redirect URI
# Must match exactly what you set in Twitter Developer Portal
# For web development: http://localhost:5173/auth/twitter/callback
# For production: https://yourdomain.com/auth/twitter/callback
# For Electron (desktop): brick://auth/twitter/callback
# For Capacitor (native iOS/Android): com.brick.app://auth/twitter/callback
# NOTE: You can add MULTIPLE redirect URIs in Twitter Developer Portal
# The app will auto-detect the environment and use the correct one
VITE_TWITTER_REDIRECT_URI=http://localhost:5173/auth/twitter/callback

# Set to 'true' if running in Capacitor (native iOS/Android app)
# The app will auto-detect Capacitor/Electron, but you can force it with this
VITE_CAPACITOR=false

# Gemini API Key (for AI features)
# Get from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
